'use client'

export const addGoogleCalendar = (accessToken: string, calendarId: string, apiKey: string, event: Object) => {
    console.log(accessToken)
  return (
    <div>addGoogleCalendar</div>
  )
}
